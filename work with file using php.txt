

Работа с текстовыми данными из php, работа с csv данными

1) http://www.php.su/articles/?cat=fs&page=005


$fp - дескриптор подключения к файлу

Для каждой отдельной операции (запись/чтение создавать свой дескриптор подключения)


 --------------------- Открытие файла
Функция  $fp =  fopen('адрес файла', 'параметры открытия')
- 'r'- Открыть только для чтения; поместить указатель в начало файла
- 'r+' - Открыть для чтения и записи; поместить указатель в начало файла
- 'w' - Открыть только для записи; поместить указатель в начало файла и усечь файл до нулевой длины. Если файл не существует, делается попытка создать его
- 'w+' - Открыть для чтения и записи; поместить указатель в начало файла и усечь файл до нулевой длины. Если файл не существует, делается попытка создать его
- 'a' - Открыть только для записи; поместить указатель в конец файла. Если файл не существует, делается попытка создать его.
- 'a+' - Открыть для чтения и записи; поместить указатель в конец файла. Если файл не существует, делается попытка создать его. 


-----------------------Функции для чтения файла

	file('адрес файла')  - Читает содержимое файла и помещает его в индексный массив
	include, require - подгружает файл
	fgets($fp, 'длинна строки (если не указана будет читать до конца)') - считывает строку из файлового указателя
	

-----------------------Функции для записи в файл



"\r\n"  -  строчка которая переводит указатель 

--------------------- Закрытия файла

Функция fclose($fp)




--------------------- Функция для работы с csv файлами
		fgetcsv($fp, 'длинна строки (если не указана будет читать до конца)', delimiter)